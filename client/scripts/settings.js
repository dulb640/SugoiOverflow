/* global angular*/
angular.module('sugoiOverflow.settings', []).value ('settings', {
	
	// A list of languages that syntax highlighting will highlight
	// OR 'all' to highlight all known languages
	highlightingLanguages: 'all'
})
